---
name: run-app
description: 启动记账 APP 的开发模式。当用户想运行、启动、打开这个记账桌面应用，或想看到代码改动的实际效果时使用。
---

# 启动记账 APP

启动记账 APP 的开发模式，按下面步骤来：

1. 用**后台方式**运行 `npm run tauri dev`（这是长驻进程，会一直开着直到关闭窗口，绝不能前台阻塞卡住）。
2. 等待 Rust 编译 + Vite 构建完成，确认桌面窗口成功弹出来。
3. 如果编译报错、或窗口没启动，把关键错误整理成大白话告诉用户，并给出下一步排查建议。
